let x = 10;
let y = 3;

x = x +5;
// is the same as
x += 5;

x = x * 3;
x *= 3;

// All mathematical operators can be combined with an equals sign to perform 
// the operation back against the variable and set it equal to the result